// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.28.0
// source: get_book_authors.sql

package database

import (
	"context"

	"github.com/google/uuid"
)

const getBookAuthors = `-- name: GetBookAuthors :many
SELECT a.full_name FROM book_authors ba
JOIN authors a ON ba.author_id = a.id
WHERE ba.book_id = $1
`

func (q *Queries) GetBookAuthors(ctx context.Context, bookID uuid.UUID) ([]string, error) {
	rows, err := q.db.QueryContext(ctx, getBookAuthors, bookID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []string
	for rows.Next() {
		var full_name string
		if err := rows.Scan(&full_name); err != nil {
			return nil, err
		}
		items = append(items, full_name)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
